// Ejemplos con String

const role = `Teacher`;

const teacherName = "Jordi";

const phrase1 = `The ${role} is ${teacherName}`;


let myObj = {
    name: 'Keyboard',
    price: 32.99,
    ISOCode: 'ES',
    units:2
}

myObj.ISOCode = 'EN' // Cambia el valor dentro del objeto.

myObj.price = myObj.price + 10 

// Para sumar una única unidad al valor de un objeto.
myObj.units++ // Es lo mismo que myObj.units = myObj.units + 1


